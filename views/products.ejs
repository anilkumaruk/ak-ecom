<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= product.name %> | AK Ecom</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f5f5f5;
    }

    .product-container {
      max-width: 800px;
      margin: auto;
      background: white;
      border-radius: 8px;
      padding: 20px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .product-container img {
      width: 100%;
      max-height: 400px;
      object-fit: contain;
      margin-bottom: 20px;
    }

    .product-container h1 {
      margin: 0;
      font-size: 28px;
    }

    .product-container p {
      margin: 10px 0;
      line-height: 1.6;
    }

    .product-container .price {
      color: green;
      font-weight: bold;
      font-size: 20px;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="product-container">
    <img src="<%= product.image || '/images/default-product.png' %>" alt="<%= product.name %>">
    <h1><%= product.name %></h1>
    <p class="price">₹<%= product.price %></p>
    <p><%= product.description || 'No description provided.' %></p>
    <p><small>Sold by: <%= product.owner.storeName %></small></p>
  </div>
</body>
</html>
